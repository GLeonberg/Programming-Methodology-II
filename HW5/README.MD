To run this program, simply navigate to the directory of the file and run:

"python heapsort.py" (ignoring quotes)

in the terminal

The dataset files MUST be in the same directory as the python source code.
